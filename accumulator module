module Accumulator (
  input CLK,
  input [7:0] bus,
  output [7:0] out,
  input La_n,
  input Ea
);
  
  reg [7:0] a;
  
  always@ (posedge CLK) begin
    if(negedge La_n) begin
      a<=bus;
    end
    
    assign out = Ea? a: 8'bz;
  
endmodule
